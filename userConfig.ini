#fedora version
fversion=20

#number of times a user can attempt to use the service before being banned
attemptLimit=3

#number of hours a user should be blocked for
blockTLimit=24

#the name of the service to be used, the string to match - Currently, only ssh is working!
#	adding a service may crash the program, if the logs for that service aren't kept in /var/log/secure or journalctl!
#services
_sshd:Failed password
_telnetd:Bad Login

#the last time the file was checked.
#this value should be set to null for first run, then left alone.
lastChecked=2014-03-13 00:54:00
